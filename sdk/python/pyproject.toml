[project]
name = "aepx"
version = "0.0.1"
description = "AEP-X Python SDK"
requires-python = ">=3.11"
dependencies = ["httpx==0.27.0"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
